/*
------------------------------------------------------------------------------ 
This code was generated by Amplication. 
 
Changes to this file will be lost if the code is regenerated. 

There are other ways to to customize your code, see this doc to learn more
https://docs.amplication.com/how-to/custom-code

------------------------------------------------------------------------------
  */
import * as common from "@nestjs/common";
import * as swagger from "@nestjs/swagger";
import { isRecordNotFoundError } from "../../prisma.util";
import * as errors from "../../errors";
import { Request } from "express";
import { plainToClass } from "class-transformer";
import { ApiNestedQuery } from "../../decorators/api-nested-query.decorator";
import * as nestAccessControl from "nest-access-control";
import * as defaultAuthGuard from "../../auth/defaultAuth.guard";
import { VacationRuleService } from "../vacationRule.service";
import { AclValidateRequestInterceptor } from "../../interceptors/aclValidateRequest.interceptor";
import { AclFilterResponseInterceptor } from "../../interceptors/aclFilterResponse.interceptor";
import { VacationRuleCreateInput } from "./VacationRuleCreateInput";
import { VacationRule } from "./VacationRule";
import { VacationRuleFindManyArgs } from "./VacationRuleFindManyArgs";
import { VacationRuleWhereUniqueInput } from "./VacationRuleWhereUniqueInput";
import { VacationRuleUpdateInput } from "./VacationRuleUpdateInput";

@swagger.ApiBearerAuth()
@common.UseGuards(defaultAuthGuard.DefaultAuthGuard, nestAccessControl.ACGuard)
export class VacationRuleControllerBase {
  constructor(
    protected readonly service: VacationRuleService,
    protected readonly rolesBuilder: nestAccessControl.RolesBuilder
  ) {}
  @common.UseInterceptors(AclValidateRequestInterceptor)
  @common.Post()
  @swagger.ApiCreatedResponse({ type: VacationRule })
  @nestAccessControl.UseRoles({
    resource: "VacationRule",
    action: "create",
    possession: "any",
  })
  @swagger.ApiForbiddenResponse({
    type: errors.ForbiddenException,
  })
  async createVacationRule(
    @common.Body() data: VacationRuleCreateInput
  ): Promise<VacationRule> {
    return await this.service.createVacationRule({
      data: data,
      select: {
        createdAt: true,
        description: true,
        id: true,
        policy: true,
        title: true,
        updatedAt: true,
      },
    });
  }

  @common.UseInterceptors(AclFilterResponseInterceptor)
  @common.Get()
  @swagger.ApiOkResponse({ type: [VacationRule] })
  @ApiNestedQuery(VacationRuleFindManyArgs)
  @nestAccessControl.UseRoles({
    resource: "VacationRule",
    action: "read",
    possession: "any",
  })
  @swagger.ApiForbiddenResponse({
    type: errors.ForbiddenException,
  })
  async vacationRules(@common.Req() request: Request): Promise<VacationRule[]> {
    const args = plainToClass(VacationRuleFindManyArgs, request.query);
    return this.service.vacationRules({
      ...args,
      select: {
        createdAt: true,
        description: true,
        id: true,
        policy: true,
        title: true,
        updatedAt: true,
      },
    });
  }

  @common.UseInterceptors(AclFilterResponseInterceptor)
  @common.Get("/:id")
  @swagger.ApiOkResponse({ type: VacationRule })
  @swagger.ApiNotFoundResponse({ type: errors.NotFoundException })
  @nestAccessControl.UseRoles({
    resource: "VacationRule",
    action: "read",
    possession: "own",
  })
  @swagger.ApiForbiddenResponse({
    type: errors.ForbiddenException,
  })
  async vacationRule(
    @common.Param() params: VacationRuleWhereUniqueInput
  ): Promise<VacationRule | null> {
    const result = await this.service.vacationRule({
      where: params,
      select: {
        createdAt: true,
        description: true,
        id: true,
        policy: true,
        title: true,
        updatedAt: true,
      },
    });
    if (result === null) {
      throw new errors.NotFoundException(
        `No resource was found for ${JSON.stringify(params)}`
      );
    }
    return result;
  }

  @common.UseInterceptors(AclValidateRequestInterceptor)
  @common.Patch("/:id")
  @swagger.ApiOkResponse({ type: VacationRule })
  @swagger.ApiNotFoundResponse({ type: errors.NotFoundException })
  @nestAccessControl.UseRoles({
    resource: "VacationRule",
    action: "update",
    possession: "any",
  })
  @swagger.ApiForbiddenResponse({
    type: errors.ForbiddenException,
  })
  async updateVacationRule(
    @common.Param() params: VacationRuleWhereUniqueInput,
    @common.Body() data: VacationRuleUpdateInput
  ): Promise<VacationRule | null> {
    try {
      return await this.service.updateVacationRule({
        where: params,
        data: data,
        select: {
          createdAt: true,
          description: true,
          id: true,
          policy: true,
          title: true,
          updatedAt: true,
        },
      });
    } catch (error) {
      if (isRecordNotFoundError(error)) {
        throw new errors.NotFoundException(
          `No resource was found for ${JSON.stringify(params)}`
        );
      }
      throw error;
    }
  }

  @common.Delete("/:id")
  @swagger.ApiOkResponse({ type: VacationRule })
  @swagger.ApiNotFoundResponse({ type: errors.NotFoundException })
  @nestAccessControl.UseRoles({
    resource: "VacationRule",
    action: "delete",
    possession: "any",
  })
  @swagger.ApiForbiddenResponse({
    type: errors.ForbiddenException,
  })
  async deleteVacationRule(
    @common.Param() params: VacationRuleWhereUniqueInput
  ): Promise<VacationRule | null> {
    try {
      return await this.service.deleteVacationRule({
        where: params,
        select: {
          createdAt: true,
          description: true,
          id: true,
          policy: true,
          title: true,
          updatedAt: true,
        },
      });
    } catch (error) {
      if (isRecordNotFoundError(error)) {
        throw new errors.NotFoundException(
          `No resource was found for ${JSON.stringify(params)}`
        );
      }
      throw error;
    }
  }
}
